# 작업 규칙

이 프로젝트에서 Claude는 아래 규칙을 지킨다.

## 의사결정
- 무언가를 **선택·결정**해야 할 때는, 곧바로 정하지 말고 **선택지를 나열하고 각 트레이드오프를 비교**한 뒤 **추천안**을 제시한다. 최종 결정은 사용자가 한다.
- 결정에 필요한 **개념이 낯설 수 있으면 먼저 쉽게 설명**한 다음 선택지를 제시한다.

## 진행
- **다음 단계로 넘어가기 전에는 반드시 사용자에게 확인**을 받고 진행한다. 임의로 다음 작업을 시작하지 않는다.
- 파일 생성·수정, 커밋, 푸시 등 **되돌리기 번거로운 작업은 실행 전 알린다**.

## 소통
- 기본 언어는 **한국어**.
- 답변은 **간결하게**, 핵심 위주로.

## 문서화
- 정리 문서는 모두 **`docs/` 폴더**에 둔다. (`CLAUDE.md`, `README.md`는 루트 유지)
- **대화·작업 성격에 맞는 문서에 기록**한다:
  - 설계·아키텍처 → `docs/design.md`
  - 선택·트레이드오프·의사결정 → `docs/tradeoffs.md` (로그 형식: `TD-001`, `TD-002` …)
  - 그 외 새로운 성격(예: 디자인/UX)이 생기면 `docs/`에 새 md를 만들어 정리한다.
- 설계·결정이 확정되거나 바뀌면 **해당 문서에 즉시 반영·기록**한다.

## 개발 방식 (TDD)
- **테스트 우선**으로 구현한다. 순서는 Red → Green → Refactor:
  1. 실패하는 테스트를 먼저 작성한다 (Red).
  2. 테스트를 통과시키는 **최소 구현**을 한다 (Green).
  3. 통과 상태를 유지하며 정리·개선한다 (Refactor).
- 기능 코드보다 **테스트를 먼저** 작성한다. 테스트 없는 기능 추가는 지양한다.
- 암호화·TOTP·만료 계산 등 **핵심 로직은 반드시 단위 테스트**로 검증한다.
